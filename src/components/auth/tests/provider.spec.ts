// mock localstorage
describe('Auth Provider', () => {
  test('provider initial values', () => {
    // authenticated should be false
    // token should be ''
  });

  test('provider gives authenticated', () => {
    // authenticated should be true
    // token should be ''
  });
});
